<?php
namespace {{ bundle }}\Seeder;

/**
 * Generated by Ginny on {{ 'now'|date('m/d/Y h:i A') }}
 *
 * https://github.com/mattcrowe/ginny
 */

use Illuminate\Database\Seeder;
use Illuminate\Database\Eloquent\Model;

class {{ bundle }}DatabaseSeeder extends Seeder {

	/**
	 * Run the database seeds.
	 *
	 * @return void
	 */
	public function run()
	{
		Model::unguard();

        {% for model in bundle.models %}
            {{ "$this->call('%s\\Seeder\\%s%sSeeder');" | format(bundle.name, bundle.name, model.name) }}
        {% endfor %}

    }

}